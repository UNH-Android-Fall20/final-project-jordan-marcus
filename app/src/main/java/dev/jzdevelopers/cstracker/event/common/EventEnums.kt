package dev.jzdevelopers.cstracker.event.common

/** Kotlin Enum EventSort,
 *  Enum for getting the ordering of the events
 */
enum class EventSort {
    LOCATION,
    NAME,
    NEWEST_TO_OLDEST,
    OLDEST_TO_NEWEST
}